# PARSER_PHP

Задача состоит из двух подзадач

## Парсинг данных с сайта

Для этого нужно заходить на страницу и делать ее дамп, дальше регуляркой вычлинять нужные данные.

### cURL

С помощью этой утилиты заходим на сайт и ташим html (в функции `fetch_html()` есть пояснения).

### Извлечение данных из html страницы

1. Идем в `div class="mehrBilder"` в котором находятся все необходимые картинки, сохраним в match.
1. Оставим только содержимое (картинки) в `$html_img_container` (из $match[1]).
1. Вычленим из `$html_img_container` ссылки на все картинки при помощи регулярки и сохраним в массив `$matches`, отсюда же вытенем только содержимое в массив `$links` (`$matches[1]`)

## Работа с excel файлами при помощи библиотеке `PhpSpreadsheet-1.12.0`

### Базовый возможности

1. Создание excel файла.
1. Добавление данных в ичейки.
1. Редактирование стиля ячейки.
1. Скачивание созданого файла.

#### Создание excel файла

Чтобы создать файл нужно:

1. Подключить библиотеку
1. Создать экземпляр класса (виртуальный excel).
1. Обратится к активной странице excel.

```php
<?php
// подключение библиотеки
requore_once "./путь_до_autoload.php";

// сокрашения 
use \PhpOffice\PhpSpreadsheet\Spreadsheet;

// создание экземпляра класса
$spreadSheet = new Spreadsheet();

// обрашаемся к активной странице
$sheet = $spreadSheet->getActiveSheet();
?>
```

#### Добавление данных в ячейки

Чтобы добавить данные нужно выполнить коснтрукцию типа:

```php
// $sheet -> setCell('адрес', 'данные');
$sheet -> setCell('A1', 'Товар');
```
